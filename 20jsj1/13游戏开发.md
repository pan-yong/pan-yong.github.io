## 游戏开发



###### 移动光标

```c
#include<windows.h>
void gotoxy(int x, int y) // 移动光标到(x,y)位置
{
    HANDLE handle = GetStdHandle(STD_OUTPUT_HANDLE);
    COORD pos;
    pos.X = x;
    pos.Y = y;
    SetConsoleCursorPosition(handle, pos);
}
```

