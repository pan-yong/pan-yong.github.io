## 2016年高考

###### 一、选择题

```tex
20.	在C语言中，函数值类型在定义时可以缺省，缺省时函数值的隐含类型是
	A. void		B. int		C. float	D. double

21.	用scanf函数输入一个字符串到数组str中，以下选项中正确的语句是
	A. scanf("%s",&str);		B. scanf("%c",&str[10]);
	C. scanf("%s",str[10]);		D. scanf("%s",str);

22.	设有定义语句:int m[ ]={2，4，6，8}，*k=m; 以下选项中表达式的值为6的是
	A. *(k+2)	B. k+2	C. *k+2	D. *k+=2
```

